= render "layouts/header"
= render "nav/nav"
.intro-box.wrap
  .ui.page.grid
    .sixteen.wide.column
      .left.floated.column
        %h2.ui.header.title 分类：#{@category[:name]}
      .right.floated.column
        #category_edit.ui.small.button
          %i.edit.icon>
          编辑
        .ui.dropdown.labeled.icon.small.button
          %i.plus.icon
          添加
          .menu
            .item 下级分类
        .ui.small.button
          %i.star.icon>
          收藏
      .clear-both
      .description
        = @category[:description]
.ui.divider
.wrap
  .ui.page.grid
    .four.wide.column
      = render "category_sorter"
    .twelve.wide.column
      = render "category_list"
= render "category_modal"
= render "layouts/footer"